# プロジェクトレビュー 2026-06-19（run 2）

- 対象: @joymerrevent/porters-connect
- ブランチ / HEAD: chore/review-2026-06-19-followups @ 7053cdb
- レビュアー: /project-review（Claude Code）
- 位置づけ: 同日 2 回目の独立した review run（前 run: [2026-06-19-01.md][run1]）。現時点のスナップショット。

## 品質ゲート（2026-06-19 08:13 実測）

| ゲート          | 結果    | 備考                   |
| --------------- | ------- | ---------------------- |
| `typecheck`     | ✅ pass |                        |
| `lint:ts`       | ✅ pass |                        |
| `lint:md`       | ✅ pass |                        |
| `format:check`  | ✅ pass |                        |
| `test:coverage` | ✅ pass | Tests 216 passed (216) |
| `build`         | ✅ pass |                        |

カバレッジ: `All files | 100 stmts | 98.74 branch | 100 funcs | 100 lines`（perFile stmts/funcs/lines=100・branch≥90 を満たす）

## 今回の指摘（サマリ）

**新規指摘ゼロ・未解決（open）ゼロ。** 台帳 [findings.md][findings] の全 RV は `fixed`。全ゲート green。

- 新規: なし
- open: なし

## 観点別所見

固定 8 観点を現時点のコード／正典に当てた結果、開いている問題はなし。退行検知のため各観点の現状を記録する。

### 1. API 忠実性 — 問題なし

カタログ駆動の decode/encode が PORTERS の値表現と一致（Option=`string[]`、User/Reference=ID-only Write、DateTime/Date=ISO⇄PORTERS）。送信前サイズガードは URL+body の合算長で Read/Write 双方を ~15000 字で弾く。OAuth は独自ヘッダ・`code_direct`・トークン ms 単位。削除 API は非提供、ホスト名は `PORTERS_HOST`（ハードコードなし）。`docs/reference/` と整合。

### 2. アーキテクチャ / ADR 遵守 — 問題なし

層分離（transport → requester → resources → xml）維持、XML は `xml/` に閉じ `resources/` へ漏れない、第1層に業務ロジックなし。モジュール依存は fields → resources の一方向（循環なし）。ADR-0004〜0024 の決定が実装に反映。

### 3. コーディング規約（ADR-0013） — 問題なし

全関数 arrow const、型は `type`、class は Error 派生と `PortersClient` のみ、`index.ts` はバレル、公開 API `src/index.ts` は明示キュレーション、ファイル名 kebab-case、`any` 不使用。lint:ts green。

### 4. 型安全 / 公開サーフェス — 問題なし

`src/index.ts` の export は意図どおり（内部型は非漏洩）。公開型名・JSDoc は英語。読み取り値・入力型は field 選択モデルと整合（optional + nullable）。`PortersClient<C>` は既定で後方互換。

### 5. エラーモデル（ADR-0006） — 問題なし

基底 `PortersError` ＋系統別サブクラス＋`category`（11 種）。未知コードは握り潰さず `unknown`。`rateLimit` は予約（未 produce）と明示。`docs/guide/error-handling.md` は `classify.ts` のマッピングと整合。

### 6. テスト / 変異テスト厳密性（ADR-0014 / 0015） — 問題なし

216 passed・perFile stmts/funcs/lines=100・branch≥90（全体 98.74）。fixture は `live-verification.md` の現行仮定（OptionRoot 等）と整合し「あり得ない応答」を前提にしていない。

### 7. ドキュメント / DX — 問題なし

README に非公式明示・契約必須・対応バージョン（Connect API v2 / PORTERS 8.x・9.x）。`createMockTransport` 例とエラーハンドリング ガイドへの導線あり。md は参照スタイル。`live-verification.md` の VERIFY(live) ↔ 表の対応は保たれている。

### 8. リリース準備 — 問題なし

`package.json` は公開メタ完備（`publishConfig.access:"public"`・`repository`/`bugs`/`homepage`/`keywords`/`author`/`license`・`exports`・`files:["dist"]`）。生成物は git 非追跡。MVP リソース（OAuth→Candidate→Job→Client→Process→Resume→Attachment）＋マスタ Read 充足。`version:0.0.0` は初回公開時に更新。

## 良かった点（strengths）

- 公開 `Transport` seam を狙う設計（mock transport が内部 fetch に非結合）＝評価レバーが薄く堅い。
- カタログ駆動の型/decode/encode を全リソースで再利用＝コアが薄い。
- フェイルセーフ徹底（未知コード→`unknown`、設定誤り→同期 `config` エラー、未モック route→明示エラー）。

## 次アクション候補

- 未解決の指摘なし。ブランチは push & PR 可能。
- 中期: README の対応 PORTERS/API バージョン明記（PRD §8・要決定）、N2 ローカル フェイクサーバー（別 ADR）、MVP 外リソース拡充。

[run1]: 2026-06-19-01.md
[findings]: findings.md
