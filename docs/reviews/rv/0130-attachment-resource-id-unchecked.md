# RV-130 🟢 添付ファイルの `resourceId`（検索と作成）を検査していない

- 重要度: 🟢 ／ 観点: フェイルセーフ
- 状態: open

## 概要

添付ファイルの `search` / `searchAll` / `create` の `resourceId` は、正の整数かを確かめずに送る。

## 根拠

- `src/resources/attachment.ts`。読んだだけ（再レビュー・2026-09-26）。

## 影響

🟢。PORTERS 側のエラーになるとみられる。

## 検出経緯

2026-09-26 の読み書きの共通の仕組みの修正（fix/accessor-review）を、change-review の手順でレビューし直したときに見つけた。止めどきの規則により、この回では直さずに記録する。

## 推奨

- `assertRecordId` と同じ検査を通す。
- 止めどきの規則（Low だけになったら直さずに記録する）により、この回では直さずに記録する。

## 処置

—
