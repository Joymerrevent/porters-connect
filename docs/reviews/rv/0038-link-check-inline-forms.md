# RV-38 🟢 リンク検査が inline リンクの一部を見ない

- 重要度: 🟢 ／ 観点: ドキュメント / フェイルセーフ
- 状態: fixed

## 概要

`pnpm check:links`（[`scripts/check-doc-links.mjs`][script]）が拾う inline リンクは
**`./` / `../` で始まるものだけ**。`[x](guide.md)` のように相対パスを直接書いた形と、
`[x](./guide.md "Title")` のようにタイトルが付いた形は**検査されないまま通る**。

## 根拠

- 実測（2026-09-12）: 仕込んだ `[x](missing6.md)` と `[x](./missing7.md "title")` は
  どちらも報告されなかった（参照スタイルの定義側は同じ日に 3 形へ拡張済み）。
- **広げると誤検出になる**ことも実測した。`./` を要求しない正規表現にすると、
  リポジトリ内に実在する**リンクではない散文**（`](…)` と `]([Field Alias],[Field Alias]...)`
  の 2 種類）をリンク先として拾う。**取りこぼしと誤検出のどちらを取るかの選択**であり、
  今は「誤検出を出さない」側を選んでいる。
  なお**このエントリ自身がその文字列を引用している**ので、素朴に grep すると件数は増える。
  ここで意味があるのは件数ではなく**種類**（実際 2026-09-12 の再レビューで、
  「実測 2 箇所」と書いた数字が自分の記述のせいで 4 になった）。

## 影響

**小さい。** inline リンクは markdownlint の `MD054`（`inline: false` / `url_inline: false`）が
そもそも禁止しているので、人が書く文書では lint が先に落とす。`MD054` の対象外は
`CHANGELOG.md`・`.changeset/`・`docs/api/` の 3 つで、**内訳は分かれる**（実測 2026-09-12）:

- `CHANGELOG.md` と `.changeset/` — 相対 inline リンクは **0 件**（外部 URL のみ）。
- `docs/api/`（TypeDoc の生成物）— 相対 inline リンクが **871 件**ある。ただし
  `check-doc-links.mjs` の `SKIP_PREFIXES` で**走査対象そのものから外れて**おり、
  ここを守るのは `pnpm check:api`（再生成して差分が出たら落とす）＝生成物には生成物の検査。

つまり現在の実害は無い。効くのは `MD054` を緩めたとき。

## 検出経緯

change-review（2026-09-12、`docs/concepts-pages`）。記法 12 通りの受理/棄却表を実データに
当てて見つけた 3 つの取りこぼしのうち、**唯一直さなかったもの**。他の 2 つ（タイトル付き定義・
3 字下げの定義）は誤検出の危険が無かったので同じ変更で塞いだ。

## 推奨

`MD054` を緩める判断をするとき、または `.changeset` / `CHANGELOG.md` に相対リンクを
書き始めたときに、この穴を塞ぐ。塞ぐなら「リンクとして成立する形」（拡張子つき、または
リポジトリ内に実在する綴り）に限定して拾い、散文を巻き込まないこと。

## 処置

**塞いだ**（2026-09-15）。推奨どおり「リンクとして成立する形」だけを拾う門（`isLinkShaped`）を
足し、`./` 無し・タイトル付き・`<…>` 囲みを検査対象に入れた。

- `./` `../` 始まりとページ内アンカーは無条件（リンク以外の解釈が無い）
- それ以外は**パスに使える字だけ**で出来ていて、かつ**拡張子が allowlist にある**もの

allowlist の中身は実測（2026-09-15。リポジトリ内のリンク先の拡張子は `.md` 1399 / `.mjs` 5 /
`.ts` 2 ／ 残りはディレクトリ指し 16 件＝すべて `./` 付き）に、将来出てきそうな隣接を足した形。
**ここに無い拡張子は `./` を付けたときだけ検査される**＝「何を見ないことにしたか」がコードに残る。

見送りの理由だった誤検出は、**実在する散文 2 種**（`](…)` と `]([Field Alias],…)` の形）を
受理ケースとしてテストに固定し、境目を残した（[RV-41][rv41]）。

## 検証

`pnpm check:links` は緑（205 ファイル・壊れリンク 0）。テストは 38 ケース green。

**このエントリ自身が引用している 2 例**（`./` 無し・タイトル付き）は
インラインコードスパンの中なので報告されない — [RV-40][rv40] を同じ PR で塞いだため。
つまり「台帳を足した瞬間に CI が赤くなる」というニアミスは、原因ごと消えている。

[rv40]: 0040-link-check-inline-code-spans.md
[rv41]: 0041-verification-harness-not-committed.md
[script]: ../../../scripts/check-doc-links.mjs
