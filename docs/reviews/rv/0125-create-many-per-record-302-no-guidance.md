# RV-125 🟢 `createMany` の 1 件ごとの Code 302 に、再送すると重複のおそれがあるという案内が無い

- 重要度: 🟢 ／ 観点: エラーモデル / DX
- 状態: open

## 概要

一括書き込みの 1 件ごとの失敗は `failed` に code 302 として返るだけで、単件の `create` のような「登録された可能性あり」の案内が無い。

## 根拠

- `src/accessor/write-many.ts`。読んだだけ（再レビュー・2026-09-26）。

## 影響

🟢。利用者が 302 を見て再送すると、重複のおそれがある。

## 検出経緯

2026-09-26 の通信と認証の修正（fix/http-auth-review）を、change-review の手順でレビューし直したときに見つけた。止めどきの規則により、この回では直さずに記録する。

## 推奨

- 件ごとの結果の説明（公開の JSDoc と文書）に、302 の扱いを書く。
- 止めどきの規則（Low だけになったら直さずに記録する）により、この回では直さずに記録する。

## 処置

—
