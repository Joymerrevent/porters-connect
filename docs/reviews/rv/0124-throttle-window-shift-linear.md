# RV-124 🟢 スロットルの時刻の列の先頭を取り除く処理が、容量に比例して遅くなる

- 重要度: 🟢 ／ 観点: 性能
- 状態: fixed

## 概要

`sent.shift()` は配列の長さに比例する。容量 20 万では 150 万回の take に 31 秒かかった。

## 根拠

- `src/http/throttle.ts` の `makeWindow`。実測（再レビュー・2026-09-26）: 容量 2000 では 150 万回で 112ms。

## 影響

🟢。PORTERS の上限（2000 / 500）では問題にならない。

## 検出経緯

2026-09-26 の通信と認証の修正（fix/http-auth-review）を、change-review の手順でレビューし直したときに見つけた。止めどきの規則により、この回では直さずに記録する。

## 推奨

- 容量が大きいときは、先頭の位置を持つ形（リングバッファ）にする。
- 止めどきの規則（Low だけになったら直さずに記録する）により、この回では直さずに記録する。

## 処置

**実施（2026-09-26・fix/http-auth-low-review・`55a44fa`）。** 送った時刻を容量の長さの輪として持ち、先頭の位置と件数で数える。容量 20 万で 150 万回の take が 19,250ms から 52ms になった（手元で測定）。

## 検証

`src/http/throttle.test.ts` の既存のテスト（どの 60 秒でも容量以下、ほか）と、src 全体のミューテーション。
