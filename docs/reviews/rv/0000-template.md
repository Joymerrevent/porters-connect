# RV-N 🔴 defect を一文で（見出しに状態や推奨は書かない）

<!-- ファイル名は NNNN-kebab-title.md（NNNN は RV-N の 4 桁ゼロ埋め）。
     この雛形をコピーして使う。ID は採番したら不変・ファイルは消さない（ADR-0052）。 -->

- 重要度: 🔴 ／ 観点: <観点（例: API 忠実性 / 型安全）>
- 状態: open

<!-- メタ 2 行は機械可読な書式を崩さない。`pnpm check:index` がここを読んで
     索引 ../findings.md と突き合わせる（重要度・観点・状態の 3 つ）。
     重要度は 🔴 High（実用ブロッカー級）/ 🟡 Medium / 🟢 Low。
     状態は open / fixed / wontfix / deferred のいずれか。
     **状態を変えたら索引の行も直す**（片方だけだと CI が落ちる）。 -->

## 概要

<何が壊れている / ズレているかを 1〜2 文で。読み手が「で、何が問題なのか」を即つかめること。>

## 根拠

<`file:line` と、API 系なら docs/reference の該当箇所。再現・追跡できない指摘は価値が低い。
「コードのこの行がこう書いてあり、正典はこう定めている」の形で対にする。>

<!-- 任意。どの作業中に・何を突き合わせて気づいたか。次に同じ穴を探すときの手がかりになる。
     不要なら節ごと削除する。 -->

## 検出経緯

<例: ADR-0044 の実装中（PR #143）。判定順の根拠を確認していて、200 側に同じ穴が残ると分かった。>

## 推奨

<処置の選択肢。複数あるなら (a) (b) で並べ、それぞれの得失を書く。
**挙動変更を伴うなら「要 ADR」と明記**する（決定を勝手に実装しない）。
契約が無いと確定できない仮定なら ../../live-verification.md の LV-N を参照し、ここには重複させない。>

## 処置

—

<!-- 未着手なら `—` のまま。確定したら「何をどう直したか」を書く（後から経緯を辿れること＝台帳の存在理由）。
     仕様として許容したなら状態を wontfix にして理由を、契約待ちなら deferred にして何待ちかを書く。 -->

<!-- 参照定義はファイル末尾にまとめる（MD054 / ユーザー規約）。
     rv/ は docs/reviews/ より 1 段深いので相対パスは ../../ 起点になる。例:

     [adr19]: ../../adr/0019-static-resource-types.md
     [lv]: ../../live-verification.md
     [run]: ../2026-08-16-01.md
-->
