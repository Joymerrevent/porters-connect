# プロジェクトレビュー 2026-06-19（run 2）

- 対象: @joymerrevent/porters-connect
- ブランチ / HEAD: chore/review-2026-06-19-followups @ 7053cdb
- レビュアー: /project-review（Claude Code）
- 位置づけ: 同日 run 1（[2026-06-19-1.md][run1]）で起票した RV-6/7/8 を同ブランチで修正した後の**再レビュー（修正検証＋退行確認）**。同日でも追記せず連番ファイルで分離する（スナップショットは点景＝後から書き換えない）。

## 品質ゲート（2026-06-19 08:13 実測）

| ゲート          | 結果    | 備考                   |
| --------------- | ------- | ---------------------- |
| `typecheck`     | ✅ pass |                        |
| `lint:ts`       | ✅ pass |                        |
| `lint:md`       | ✅ pass |                        |
| `format:check`  | ✅ pass |                        |
| `test:coverage` | ✅ pass | Tests 216 passed (216) |
| `build`         | ✅ pass |                        |

カバレッジ: `All files | 100 stmts | 98.74 branch | 100 funcs | 100 lines`（run 1 比 branch 98.48→98.74＝`mock-transport.ts` が 100% へ）

## 今回の指摘（サマリ）

新規指摘ゼロ・未解決ゼロ。run 1 の RV-6/7/8 を本ブランチで解消し、独立検証で `fixed` を確認。

| ID   | 重要度 | 観点              | 概要                                                                         | 状態  |
| ---- | ------ | ----------------- | ---------------------------------------------------------------------------- | ----- |
| RV-6 | 🟢     | テスト厳密性      | `MockReply` オブジェクト形（status 省略→200）の pin テスト追加。branch 100%  | fixed |
| RV-7 | 🟢     | ドキュメント / DX | 未モック route エラーを `GET /v1/job`（method+pathname・クエリ省略）に簡潔化 | fixed |
| RV-8 | 🟢     | アーキテクチャ    | `EmptyCatalog` を `read-core` へ移し依存方向を fields→resources に統一       | fixed |

- 新規: なし
- 状態変化: RV-6 / RV-7 / RV-8 open→fixed（run 1 の 🟢 Low 3 件を解消・下記で独立検証）

## 観点別所見（修正差分に当てた結果）

固定 8 観点を RV-6/7/8 の修正差分（コミット `727b46e` / `2edd134`）に当てた。退行検知のため指摘ゼロも記録する。

### 1. API 忠実性 — 問題なし

`routeLabel` はエラーメッセージ整形のみで実 API 挙動に非干渉。`EmptyCatalog` 移設は型のみで decode/encode・URL 生成に変化なし（typecheck/test green）。

### 2. アーキテクチャ / ADR 遵守 — 問題なし（RV-8 で改善）

grep で独立確認: `src/resources/` からの `../fields` import は消滅、`fields → resources` は `define-fields.ts:8` の `import type`（実行時循環なし）のみ、`read-core` は fields 非依存。上向き依存が解消し方向が是正された。層分離（http/resources/xml）も維持。

### 3. コーディング規約（ADR-0013） — 問題なし

`routeLabel` は arrow const、新規 class なし、型は `type`、`any` 不使用。`no-use-before-define` 順序維持。lint:ts green。

### 4. 型安全 / 公開サーフェス — 問題なし

`EmptyCatalog` は `src/index.ts` に非漏洩（grep 確認）。公開 export・`PortersClient<C>` の後方互換に変化なし。

### 5. エラーモデル（ADR-0006） — 問題なし

未モック route は引き続き `PortersConfigError`（`category:"config"`）。メッセージ整形のみで分類・系統は不変。

### 6. テスト / 変異テスト厳密性 — 問題なし（RV-6 で改善）

`mock-transport.test.ts` に「status 省略→200」「不正 URL フォールバック」の 2 本を追加し、`mock-transport.ts` が lines/stmts/branch とも 100% に。216 passed。

### 7. ドキュメント / DX — 問題なし（RV-7 で改善）

未モック時メッセージが `GET /v1/job` と簡潔化（`pnpm sandbox` 実機確認）。README/ガイドへの影響なし。

### 8. リリース準備 — 問題なし

`package.json`・追跡物に変化なし（修正は src/ と docs/reviews のみ）。MVP リソース充足は run 1 から不変。

## 良かった点（strengths）

- 🟢 Low の指摘でも fail-safe を崩さず処置（RV-7 は不正 URL の raw フォールバックを残し、その経路まで pin）。
- RV-8 は「動くが将来困る」依存方向を、挙動を変えずに型移設だけで是正＝薄く堅い修正。
- 指摘 → 修正 → 再レビューの各段で台帳（findings）に状態と処置が残り追跡可能。

## 次アクション候補

- 未解決の指摘なし。ブランチ `chore/review-2026-06-19-followups` は push & PR 可能。
- 中期: README の対応 PORTERS/API バージョン明記（PRD §8・要決定）、N2 ローカル フェイクサーバー（別 ADR）、MVP 外リソース拡充。

[run1]: 2026-06-19-1.md
