# プロジェクトレビュー 2026-09-21（run 2）

- 対象: @joymerrevent/porters-connect（公開済み **0.21.0**）
- ブランチ / HEAD: `develop` @ `535b494`
- レビュアー: /project-review（Claude Code）
- 位置づけ: 同日 2 回目の独立した run（前 run: [2026-09-21-01][run1]・0.20.0 @ `566d330`）。
  以降 **22 コミット**・2 リリース（0.20.1 ＝ RV-55〜57 の処置／**0.21.0 ＝ [ADR-0087][adr87]
  実施＝破壊的変更 1**）・ADR **1 本**追加（0087）・検査 **3 本**追加（`check:api` の拡張・`check:dts`・
  `check:usage`）・テスト 1354 → **1414 件**。前 run の open 3 件は 0.20.1 ですべて fixed になり、
  本 run 開始時の台帳は open **0 件**。本 run は **0.21.0 の 3 つの変更**（宣言の `tenant()` への移動・
  公開 JSDoc と利用者向け文書からの識別子退避・`docs/usage` の一斉洗い）を出典と実装に突き合わせた。

## 品質ゲート

### 品質ゲート（2026-09-21 19:47 実測）

| ゲート          | 結果    | 備考                     |
| --------------- | ------- | ------------------------ |
| `typecheck`     | ✅ pass |                          |
| `lint:ts`       | ✅ pass |                          |
| `lint:md`       | ✅ pass |                          |
| `format:check`  | ✅ pass |                          |
| `check`         | ✅ pass |                          |
| `test:coverage` | ✅ pass | Tests 1414 passed (1414) |
| `build`         | ✅ pass |                          |
| `check:publish` | ✅ pass |                          |
| `check:dts`     | ✅ pass |                          |

カバレッジ: `All files | 100 | 99.26 | 100 | 100`（stmts / branch / funcs / lines）

**9 本すべて緑**。前 run の 8 本に **`check:dts`**（配布する `dist/index.d.ts` の JSDoc 規約・
0.21.0 で追加・`gates.sh` は #364 で既に載せている）が加わった。`check` の束ねは `check:usage` が増えて 9 本
（`pnpm check:lv` → `未確認 28 ／ 確定 1 ／ 解消 2`・前 run と同じ）。本 run の指摘 4 件は
**すべてゲートの網の外側**。

> ミューテーション（Stryker）は本 run で**全体を再実測**した（19:48〜19:50・2 分 41 秒）:
> **96.59**（2650 ミュータント・killed 2510・timeout 9・**Ignored（`// Stryker disable` で明示）42**・
> **Survived 89**・閾値 95 ＝ 緑）。前 run の 96.45 ／ survived 92 から、[RV-55][rv55] の処置で
> `time-of-day.ts` の 3 件が killed になった分だけ動いた。
>
> **前 run と違うのは、survivor の「中身」を出したこと**。ファイル別は `xml/decode.ts` 20・
> `auth/auth-api.ts` 20・`resources/image.ts` 15・**`client.ts` 10**・`resources/query.ts` 7・
> `expand.ts` 4・`encode.ts` 4・`field-type.ts` 3・`mock-transport.ts` 2・`resource.ts` 2・
> `throttle.ts` 1・`option.ts` 1。前 run はこれを「いずれも前回までに equivalent と判断済み」と書いたが、
> **`client.ts` の 10 件は L349〜359 のリソースキー（`customFor("job")` …）の文字列変異で、同値ではない**
> （手で変異させて 1414 件が緑のまま通ることを実測）→ [RV-59][rv59]。
> 前 run の記述は根拠の無い一般化だった（`docs/reviews/` に判断の記録は無い）。
> なお `reports/mutation/mutation.json` は 09-20 の `parser.ts` 単体の古い結果のまま
> （全体の数字は HTML レポートに埋め込まれた JSON から取った）。

## 普及シグナル（この run 時点）

PRD §7 の遅行指標の控え（[ADR-0084][adr84]）。**GitHub の traffic は 14 日で消える**ので、
ここが唯一の記録になる。

| 指標          | 実測                                                                                    |
| ------------- | --------------------------------------------------------------------------------------- |
| traffic views | **411** / uniques **23**（直近 14 日 = 09-07〜09-20）                                   |
| 流入元        | `github.com` 18/6 ・ **`npmjs.com` 8/1** ・ **`Google` 3/1** ・ `bestpractices.dev` 1/1 |
| 外部 Issue/PR | **0 件**（open の Issue / PR とも 0。直近 30 日の作成者は Dependabot と自分だけ）       |
| Stars / Forks | 1 / 0                                                                                   |

同日の前 run（09:00 頃）から窓が 1 日ずれただけ（09-06 の 112 views / 1 unique が窓から落ち、
09-20 の 38 / 1 が入った）。**判定基準は変わらず保留**: `npmjs.com` の uniques は **1 のまま**、
`Google` も 3/1 のまま。**09-18 の uniques 17 は窓の中で依然 1 日だけの突出**で、その後 09-19 / 09-20 は
uniques 1 に戻っている＝再現していない。次 run（09-18 が窓から落ちる 10-02 以降）で 14 日の合計
uniques がどこまで下がるかで、17 のうち「人」がどれだけいたかが逆算できる。

npm の日次ダウンロードは 09-16 = 172・09-18 = 168・09-20 = 309（0.19.1 / 0.20.0 の公開日）で、
前 run に書いた「公開日だけ跳ねる＝レジストリのスキャナ」の形のまま。0.21.0（09-21 10:38Z 公開）の
分は明日以降に出る。

## 今回の指摘（サマリ）

重要度順（問題が先）。**状態は本 run 時点**のもので、現在の状態は[台帳の索引][findings]が正。
各 ID は台帳の本体へリンクする（詳細はそちら）。

| ID            | 重要度 | 観点                        | 概要                                                                                 | 状態（当時） |
| ------------- | ------ | --------------------------- | ------------------------------------------------------------------------------------ | ------------ |
| [RV-59][rv59] | 🟡     | テスト厳密性 / プロセス     | `tenant(id, { fields })` の宣言配線を candidate 以外の 10 リソースで pin していない  | open         |
| [RV-58][rv58] | 🟢     | API 忠実性 / フェイルセーフ | `Number` の Read が数値でない文字列を `NaN` に黙って変換し、そのまま Write に戻る    | open         |
| [RV-60][rv60] | 🟢     | ドキュメント / API 忠実性   | 0.21.0 の CHANGELOG と ADR-0087 が 0.15.0 で直した「黙って `null`」を現在形で書く    | open         |
| [RV-61][rv61] | 🟢     | ドキュメント / プロセス     | roadmap が reference README の「再取得の手順」を指すが、その節は CONTRIBUTING に移動 | open         |

- 新規: RV-58, RV-59, RV-60, RV-61
- 状態変化: なし（本 run 開始時に open が 0 件だったため、再評価の対象が無かった。前 run の
  RV-55〜57 は 0.20.1 で fixed 済み・検証節を読んで確認）

🟡 1・🟢 3。**4 件のうち 3 件（RV-58 / 59 / 60）は 0.21.0 の主題「宣言の取り違え」の周辺**で見つけた。
0.21.0 は「別テナントの宣言が黙って適用される状態を設計で無くす」版だが、(1) その動機の記述が
RV-36 以前の挙動のまま（RV-60）、(2) RV-36 の後にも残っている silent な経路が 1 つある（RV-58）、
(3) 新しい配線を守るテストが 1 リソース分しかない（RV-59）。**決定（案B）自体は正しい**
（層の整合・トークン共有・型引数の単純化で立つ）。

## 観点別所見

> **指摘の詳細（概要・根拠・推奨・処置）は台帳の各 RV ファイルが正**（[ADR-0054][adr54]）。
> ここには**その回でしか分からないこと**（何を突き合わせて気づいたか・判断の背景・
> 退けた解釈・退行監視のための「問題なし」の記録）を残す。

固定の 8 観点を番号順で通す。

### 1. API 忠実性 — 🟢 [RV-58][rv58]

**突き合わせたもの**: 0.21.0 で書き換わった `docs/usage/reference/` 9 ファイルの差分を 1 行ずつ。
識別子の HTML コメント化が大半で事実は動いていないが、`gotchas.md` に**挙動の言い換え**が 3 か所
入っていたので、それぞれ実装と突き合わせた:

- 「ライブラリは未知の alias を落とさず（応答に混ざっても無視する）、宣言との食い違いは `validation`
  で知らせる」— `decode.ts:341` / `:358` の `mismatch` throw と一致（[RV-36][rv36]）。**正しい**。
- 「バックオフ付きで再試行する（非冪等な `create` は除く）」— `requester.ts` の冪等ガードは
  「送信済みかつ結果不明」のときだけ create を止め、429 と送信前失敗は再送する（[RV-22][rv22]）。
  簡略化だが、詳細へのリンク付きなので**許容**。
- 「エッジ / サーバーレス実行環境での動作は確認していない」— 以前の「非推奨と README に明記する」
  （設計メモ調）からの書き換え。**正しい**（確認していない、が事実）。

**気づいた経緯（RV-58）**: 上の 1 つ目を確かめた直後に、同じ日の CHANGELOG 0.21.0 が逆のこと
（「黙って `null`」）を言っているのに気づき（→ RV-60）、「では RV-36 の後に残っている silent な経路は
何か」を `decodeField` のスカラ分岐で 1 型ずつ追った。日時は `converted()`・文字列型は無変換・
**`Number(value)` だけが検査なし**。`"abc"` → `NaN` を実測し、Write 側の `String(NaN)` も追った。
**退けた解釈**: 「PORTERS が Number 項目に数値以外を返すことは無いから不要」— そのとおりで、
だから 🟢。ただし到達経路は 0.21.0 が動機にした「別テナントの宣言を当てる」そのものであり、
[ADR-0069][adr69] の実測表がこの向き（テキスト / `f.number()`）を**測っていなかった**ことが、
RV-36 でも拾えなかった理由。

**0.21.0 の本体（宣言の `tenant()` 移動）は API 忠実性に影響しない**（XML・クエリ・ヘッダは不変）。
出典との対応は「カスタム項目はテナント毎」（[resources-list][ref-list]）に**近づいた**側。

### 2. アーキテクチャ / ADR 遵守 — 問題なし

[ADR-0087][adr87] の「実施時の合意事項」1〜7 を全数突合し、**すべて実装に入っている**:
(1) `PortersClientOptions` から `fields` 削除・型引数外し・`customFor` を `buildScope` 内へ／
(2) `tenant` ジェネリック化・`TenantOptions` を `src/index.ts` から export／(3) 型テスト 4 種
（`custom-fields.test.ts:38-70`）＋実行時テスト（candidate・**RV-59 はここの範囲の話**）／
(4) CHANGELOG 最上部に破壊的変更と 1 対 1 の移行／(5) ガイド 2 本・概念 1 本・基本設計・examples・
README・ADR-0005 / 0023 の Amended 注記／(6) フェイク・`verifyFields` 等は**コメント以外の差分ゼロ**
（`git diff` で確認）／(7) `fields?: never` ＋ 構築時 `PortersConfigError`。

**判断の背景**: 合意事項 7 は実装時のレビューで足したもの（ADR に「追記（実装時）」として残っている）。
`fields: undefined` を未指定と同じに扱う判断（任意の spread を落とさない）は `handle-failures.md:239`
と `custom-fields.test.ts:196` の両方で pin されている。

層分離: `client.ts` が `errors` を import するようになったが、これは構築時の設定エラー
（[ADR-0048][adr48] の系列）で `hostname` と同じ位置。XML が `resources/` に漏れていないことは
前 run と同じ（`XMLParser` は `parser.ts` の 1 つだけ）。

### 3. コーディング規約（ADR-0013） — 問題なし

機械的に実測して全項目 clean（前 run と同じ手順）: `any` **0**・`interface` **0**・class は
`PortersClient` ＋ Error 派生 5 の **6 件**・`function` 宣言 **0**・`index.ts` 8 本の宣言 **0 行**・
kebab-case 違反 **0**。`PortersClient` が非ジェネリックになり、公開型の型引数は `TenantScope<C>` /
`TenantOptions<C>` の 2 つに減った（[ADR-0087][adr87] の狙いどおり）。

### 4. 型安全 / 公開サーフェス — 問題なし

`src/index.ts` の差分は `TenantOptions` の追加のみ。`check:api` 緑＝生成リファレンスは 189 → **195 頁**
（`TenantOptions` と、typedoc が拾い直した型）。日本語（かな）・保守者向け識別子の混入は
`check:api` / **`check:dts`** の両方で 0。

**確認して問題なしとしたこと**: `check:dts` が見る `dist/index.d.ts` は typedoc に出ない 43 ブロック
（`Resource` の各メソッド等・IDE のホバーに出る）を含む。`#363` の自己レビューで見つけた穴で、
**dist が無ければ落とす**（「無いので何も見なかった」を緑にしない）設計になっている。CI では
`build` の後に置かれ、`pnpm check` の束ねには**意図して入れていない**（`check:publish` と同じ理由）。
`gates.sh` も同じ位置に置いている。

### 5. エラーモデル — 問題なし

新規の throw は `PortersClient` コンストラクタの `PortersConfigError`（`category: "config"`・hint は
`porters.tenant(id, { fields })`）1 つ。`src` で `PortersError` 派生以外を throw するのは
`util/datetime.ts` の `RangeError` 5 箇所のまま（呼び出し側で包む・[RV-36][rv36]）。
0.21.0 Fixed の「hint が廃止済みの `host` を案内していた」は `classify.ts` で `hostname` / `port` /
`scheme` に直っており、`hint:` に `host` 単独で残る箇所は **0 件**。

RV-58 は「エラーを出すべき所で出していない」指摘だが、原因は変換の検査範囲（観点 1）なので
そちらに置いた。

### 6. テスト / 変異テスト厳密性 — 🟡 [RV-59][rv59]

1414 件・perFile 100/99.26/100/100 で閾値（[ADR-0014][adr14]）を満たす。0.21.0 で足した検査
スクリプト 4 本（`check-api-reference` の拡張・`check-dts-comments`・`check-release-invariants`・
`check-usage-docs`）は**すべて隣に `.test.mjs` がある**（1:1）。

**判断の背景（RV-59）**: 前 run は survivor を「前回までに equivalent と判断済み」と書いて中身を
見なかった。今回は HTML レポートの埋め込み JSON から行を出した。`client.ts` の 10 件が
**L349〜359 のリソースキー**で、コメントでも hint 文字列でもなかった。同値であるためには
`customFor("")` と `customFor("job")` の挙動が同じでなければならず、`scope.fields?.[""]` は常に
`undefined` なのであり得ない。手で L349 を変異させて全件回し、**1414 件緑**を確認した。
🟡 にしたのは「テストの穴」よりも「**ゲートが穴を見なくなっていた**」ほう。[ADR-0015][adr15] は
「survived は撃破するか `// Stryker disable` ＋理由で明示するか」と決めているが、Ignored 42 に対して
Survived 89 が**どちらでもない**まま閾値 95 の下に溜まっている。RV-59 の推奨 (b) はその振り分け。

**退けた解釈**: 「`test/integration/tenant-fields.test.ts` や `generateFieldDecls` のテストに job 以下の
宣言があるから守られている」— 宣言の**生成・検証**は `tenant(id)` スコープを取るが、`buildScope` の
`customFor` を**通らない**（Field Read を直接読む）。配線を通るのは `search` / `get` の decode だけ。

### 7. ドキュメント / DX — 🟢 [RV-60][rv60] ／ 🟢 [RV-61][rv61]

README は非公式明示（`:6` / `:9` / `:138`）・契約必須（`:10` / `:30`）・対応 API version（`:121`）が
そろい、170 行。構築例に `fields` は無く、`tenant(456)` を「単一テナントでもこの形」と案内している。
ガイドは [ADR-0087][adr87] 合意事項 5 のとおり書き換わり、「テナント別 client」は**認証を分けたい場合**
だけの案内になった（`multi-tenant.md:73-76`）。`TenantOptions` の型引数なしの落とし穴も
「節ごと削除」でなく**書き換えて残した**（ADR の「訂正（実装時）」と一致）。

**`check:usage` の設計を確認**: allowlist（`docs/README.md` と fake-server-runbook のみ外部リンク可）・
HTML コメントを剥がしてから本文を見る・inline リンクも見る。denylist でない理由が書かれている
（新しい保守者向けディレクトリが黙って通らない）。**CHANGELOG は対象外**＝ RV-60 の記述は
どの検査にも掛からない。

**気づいた経緯（RV-60）**: 観点 1 の突き合わせで `gotchas.md` の「`validation` で知らせる」を正しいと
確認した直後に、同じ日に書かれた CHANGELOG 0.21.0 が「黙って `null`・例外も警告も出ません」と
逆のことを言っているのに気づいた。`grep '黙って'` で現在形のものを洗い、ADR-0087 → ADR-0069
（2026-09-09）→ RV-36 の処置（2026-09-10）と日付を並べて「起票前日の実測表を 12 日後に再検証せず
引いた」と特定した。ADR-0069 の記述は歴史的に正しい。**0.15.0 の CHANGELOG が正しく、0.21.0 が
それと矛盾している**のが指摘の核。

**気づいた経緯（RV-61）**: roadmap の V5 節から「再取得の手順」を reference README に探して無く、
HTML コメントで CONTRIBUTING への移動を知った。#365 の変更ファイルに roadmap が無い＝**文書の移動に
対する洗いが `docs/usage/` の外まで及ばなかった**。[破壊的変更のドキュメント洗い][adr78] と同じ類型。

LV は `未確認 28 ／ 確定 1 ／ 解消 2` で `check:lv` 緑・roadmap V4 / V5 と一致。`VERIFY(live)` は
39 箇所（前 run と同数）。

### 8. リリース準備 — 問題なし

`package.json` は 0.21.0・`publishConfig.access: "public"`・`exports` に `require` 条件・
`engines >= 22.12.0`・metadata 完備。`.changeset/` は空（README / config のみ）・タグ `v0.21.0`・
npm は `0.21.0`（2026-09-21T10:38Z 公開）で三者が一致。生成物（`dist/` `coverage/` `reports/` `tmp/`）
の git 追跡は **0 件**。CI マトリクスは `["22.12.0", 22, 24, 26]` のまま。Dependabot の actions 4 本・
dev-deps 3 本は取り込み済み（#332 / #333）で open の PR は 0。

## 良かった点（strengths）

- **前 run の 3 件が 0.20.1 で同日に fixed**。RV-55 の検証節は「指摘の表と同じ入力を修正後に実測」
  の形で、読むだけで再評価が終わった。RV-56 は再発防止を `check:release` に載せ、索引の凡例まで
  書き換えている（**任意項目が運用から落ちる**類型を仕組みで止めた）。
- **ADR-0087 が実装時の訂正を ADR に残している**。「ガイドの落とし穴が節ごと消える」を打ち消し線で
  訂正し、`TenantOptions` に移ると書き直した。決定の文面は変えず、根拠の誤りを隠さない。
  RV-60 の推奨は**この形に揃える**だけでよい。
- **識別子の退避を「生成物 2 経路 ＋ 手書き 1 経路」で塞いだ**。`check:api`（typedoc 出力）だけでは
  IDE のホバーに出る 43 ブロックが見えないことを自己レビューで実測し、`check:dts` を足した。
  「検査したつもりで見ていない生成物」を数えた点が、[RV-42][rv42]（ゲート一覧の分散）と同じ目。
- **`gotchas.md` の書き換えが設計メモ調から事実の記述に変わった**。「〜を検討」「〜する必要がある」が
  「ライブラリは〜する」に直り、その 3 か所とも実装と一致していた（観点 1）。
- **前 run の誤りを本 run で見つけられた**（RV-59）。「survivor N 件・前回と同じ」を「中身も同じ」と
  読まないこと。数字の一致は退行が無い証拠にはなるが、**最初から穴だった**ことの証拠にはならない。

## 次アクション候補

1. **[RV-59][rv59]（🟡）を先に**。11 リソースの表駆動テスト 1 本で配線を pin する（ADR 不要・
   半日未満）。推奨 (b) の survivor 79 件の振り分けは別 PR で、ADR-0015 の規則と実態の
   どちらを直すかを決めてから。
2. **[RV-58][rv58]**。`Number` / `System[Id]` の Read に `Number.isFinite()`、Write にも同じ検査。
   RV-36 の日時と同じ形なので ADR 不要。敵対的入力で pin し、[ADR-0069][adr69] の実測表に
   「テキスト / `f.number()`」の行を足す。patch で出せる。
3. **[RV-60][rv60] ＋ [RV-61][rv61]** は文面の修正だけ。RV-60 は CHANGELOG 0.21.0 の括弧内と
   ADR-0087 Context への「訂正」追記、RV-61 は roadmap の 1 語。同じ docs PR で出せる。
4. **普及シグナルは次 run でも同じ 2 点**: `npmjs.com` の uniques（今回も 1）と、09-18 の 17 が窓から
   落ちた後（10-02 以降）の 14 日合計 uniques。0.21.0 の npm 日次も控える。
5. 1.0.0 の V3 は **open 4 件**で一時的に未達に戻る。4 件とも自分で進められる（契約不要）ので、
   次のマイルストーンは変わらず**契約後のライブ検証（V5）**。

[adr14]: ../adr/0014-test-coverage-policy.md
[adr15]: ../adr/0015-mutation-testing.md
[adr48]: ../adr/0048-access-point-host-validation.md
[adr54]: ../adr/0054-snapshot-ledger-role-split.md
[adr69]: ../adr/0069-tenant-field-catalog-tooling.md
[adr78]: ../adr/0078-hostname-port-split.md
[adr84]: ../adr/0084-success-metrics-measurement.md
[adr87]: ../adr/0087-tenant-scoped-field-declarations.md
[findings]: findings.md
[ref-list]: ../usage/reference/resource-api/resources-list.md
[run1]: 2026-09-21-01.md
[rv22]: rv/0022-ratelimit-create-no-retry.md
[rv36]: rv/0036-write-value-validation-partial.md
[rv42]: rv/0042-quality-gate-list-drift.md
[rv55]: rv/0055-time-of-day-decode-hour-unchecked.md
[rv58]: rv/0058-number-decode-nan-unchecked.md
[rv59]: rv/0059-tenant-fields-threading-unpinned.md
[rv60]: rv/0060-changelog-stale-silent-null-premise.md
[rv61]: rv/0061-roadmap-points-to-moved-section.md
