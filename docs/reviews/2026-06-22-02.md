# プロジェクトレビュー 2026-06-22（run 2）

- 対象: @joymerrevent/porters-connect
- ブランチ / HEAD: feat/monotonic-check-release-scope @ 6572d64（PR #82・develop へマージ待ち）
- レビュアー: /project-review（Claude Code）
- 位置づけ: 同日 2 回目の独立した run（前 run: [2026-06-22-01.md][run1]）。run1 で起票した RV-9 の修正（ADR-0032 実装）が入った状態の確認スナップショット。

## 品質ゲート（2026-06-22 12:21 実測）

| ゲート          | 結果    | 備考                   |
| --------------- | ------- | ---------------------- |
| `typecheck`     | ✅ pass |                        |
| `lint:ts`       | ✅ pass |                        |
| `lint:md`       | ✅ pass |                        |
| `format:check`  | ✅ pass |                        |
| `test:coverage` | ✅ pass | Tests 230 passed (230) |
| `build`         | ✅ pass |                        |

カバレッジ: `All files | 100 stmts | 98.74 branch | 100 funcs | 100 lines`（perFile stmts/funcs/lines=100・branch≥90 を満たす）。テストは run1 の 229→230（ADR-0032 の「非 release 文脈で単調増加 skip」テスト 1 件増）。

## 今回の指摘（サマリ）

**新規指摘ゼロ。** 前 run の RV-9 が `fixed`（このブランチ／PR #82）になり、台帳 [findings.md][findings] の全 RV-1〜9 が `fixed`。全ゲート green。

| ID   | 重要度 | 観点                      | 概要                                                       | 状態  |
| ---- | ------ | ------------------------- | ---------------------------------------------------------- | ----- |
| RV-9 | 🟡     | アーキテクチャ / リリース | 単調増加チェックを base=main の PR に限定（ADR-0032 実装） | fixed |

- 新規: なし
- 状態変化: RV-9 open→fixed（ADR-0032 案A を実装＝PR #82。develop へは未マージ）

## 観点別所見

固定 8 観点を番号順（1→8）で当てる。run1 以降 **`src/` ライブラリ本体は不変**（差分は ADR docs・`scripts/`・台帳のみ）なので、観点 1/3/4/5/7 は run1 の所見を維持し、退行が無いことを確認した。

### 1. API 忠実性 — 問題なし

`src/` に変更なし（`git diff 3a82ef1..HEAD -- src` 空）。カタログ駆動 decode/encode（Option=`string[]`、User/Reference=ID-only Write、DateTime/Date=ISO⇄PORTERS）、送信前サイズガード（URL+body 合算 ~15000 字）、OAuth 独自ヘッダ・`code_direct`・トークン ms 単位、削除 API 非提供、ホスト名 `PORTERS_HOST` は run1 から不変で `docs/reference/` と整合。`VERIFY(live)` ↔ LV 対応も維持。

### 2. アーキテクチャ / ADR 遵守 — RV-9 fixed（良化）

run1 の RV-9（単調増加チェックが back-merge 窓で develop の無関係 PR を誤検知）を **ADR-0032（accepted・案A）** で解消。実装は `scripts/check-release-invariants.mjs`:

- 単調増加(2)を `releaseContext`（`process.env.GITHUB_BASE_REF === "main"`・`:108` 付近）ガード下に置き（`:84` の判定に `releaseContext &&` を追加）、**base=main の PR でのみ**検査。develop の通常 PR・push・local では skip。
- 形式検証(1)・baseline（直近 git タグ）・自前比較・`fetch-tags` は **ADR-0031 のまま不変**。版の逆行は publish 経路（必ず main 向け PR を通る）で確実に弾くため実害なし、CI も据え置き。
- 検査ロジックは引き続き純粋関数（`releaseContext` を引数注入）でテスト可能性を維持。

ADR 整合も良好：accepted な ADR-0031 は本文を書き換えず、(2) の実行範囲が ADR-0032 で superseded された旨の注記を1行追記（ADR-0029→0030 と同じ作法）。層分離・依存方向（fields → resources・循環なし）は run1 から不変。**残る軽微な留意点**（指摘化はしない）：直接 push（main への hotfix 直 push 等）では `GITHUB_BASE_REF` が空で (2) を検査しないが、main は PR 必須（branch protection）で release も PR 経由のため publish 経路は守られる＝ADR-0032 の設計どおり。

### 3. コーディング規約（ADR-0013） — 問題なし

`src/` 不変。新規・改修した `scripts/*.mjs` は規約対象外のビルドスクリプトだが arrow 関数・named export・`process.env` 読みで eslint green（`**/*.mjs` は type-checked 無効化）。`any` 不使用。

### 4. 型安全 / 公開サーフェス — 問題なし

`src/index.ts` の公開 export は run1 から不変（内部型は非漏洩・公開型/JSDoc は英語）。RV-9 修正は `scripts/`・docs のみで公開サーフェスに影響なし。

### 5. エラーモデル（ADR-0006） — 問題なし

`PortersError` ＋系統別＋`category`（`rateLimit` は予約明示）に変更なし。`check-release-invariants.mjs` はライブラリのエラーモデル外（ビルド時 `process.exit(1)`＋人間向けメッセージ）で妥当な分離を維持。

### 6. テスト / 変異テスト厳密性（ADR-0014 / 0015） — 問題なし

230 passed・perFile 100/100/100・branch 98.74。RV-9 修正で「release 文脈（base=main）で逆行検知／非 release 文脈で skip」の両分岐を pin するテストを追加（`check-release-invariants.test.mjs`・`ok` fixture に `releaseContext: true`）。ロジックを引数注入の純粋関数に保ったことで両文脈をユニットで確実に検証。`src/` の fixture は実挙動と乖離なし。

### 7. ドキュメント / DX — 問題なし

README は非公式明示・契約必須・対応バージョンを維持。ADR-0032 が追加され決定の追跡性が向上。`docs/release-runbook.md` の半自動フロー記述と実装（base=main 限定）は整合。md は参照スタイル。`live-verification.md` の VERIFY(live) ↔ 表の対応は保たれている。

### 8. リリース準備 — 問題なし

`package.json` は公開メタ完備（`publishConfig.access:"public"`・各メタ・`exports`・`files:["dist","CHANGELOG.md"]`）。version=0.2.0（v0.2.0 公開済み）。生成物（coverage/reports/tmp/dist）は git 非追跡を確認。`release.yml`（OIDC publish）・`tag.yml`（自動タグ）・`check:release`（形式常時＋単調増加は release PR 限定）でリリース経路の自動ガードが整った。MVP リソース＋マスタ Read は充足のまま。

## 良かった点（strengths）

- RV-9 を「ADR 起票（proposed→accepted）→ 別 PR で実装 → 台帳 fixed」の正規フローで処理＝決定の追跡性とフェイルセーフ運用が機能している。
- 単調増加検査を「実害が出る瞬間（publish＝main 向け PR）」に絞り、誤検知（develop の back-merge 窓）を排除しつつ本来の防御は保つ＝薄く堅い調整。
- 検査ロジックを引数注入の純粋関数に保ち、CI 文脈依存（`GITHUB_BASE_REF`）を `main()` 境界に閉じ込めた＝テスト容易性を犠牲にせず文脈分岐を実現。
- ライブラリ本体は連続して安定（run1 から `src/` 変更ゼロ・全 RV fixed）。

## 次アクション候補

- 未解決の指摘なし。**PR #82 をマージ**すれば develop の RV-9 も fixed に揃う（現状ブランチ先行）。
- 中期: README の対応 PORTERS/API バージョン明記（PRD §8・要決定）、MVP 外リソース拡充、N2 ローカル フェイクサーバー（別 ADR）。

[run1]: 2026-06-22-01.md
[findings]: findings.md
