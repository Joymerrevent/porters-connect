---
"@joymerrevent/porters-connect": patch
---

**API リファレンスの `CandidateResource` などデータ系 12 種の型のページに、各メソッド（`search` / `searchAll` / `get` / `getMany` / `create` / `update` / `createMany` / `updateMany`）の引数・戻り値・説明を載せました**。型の中身は変わりません。
