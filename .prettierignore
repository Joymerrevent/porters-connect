# 依存・成果物
node_modules
dist
build
coverage
pnpm-lock.yaml
package-lock.json
yarn.lock

# ツール設定（Claude Code のローカル設定など）
.claude

# TypeDoc の生成物（ADR-0068）。整形すると typedoc の出力と食い違い、
# `pnpm check:api` の再生成差分ゲートが常に落ちる。生成物はそのまま置く。
docs/usage/api

# 手書きの仕様/設計ドキュメントを整形対象外にしたい場合は追記する
# 例:
# SPEC.md
# DESIGN.md
